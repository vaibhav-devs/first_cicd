name: commit_checkout_1
on: [push]
jobs:
  external-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: newCheckouttest
        uses: actions/checkout@v3
        with:
          repository: abystoma/external-workflow
          ref: branch
      - name: Greetings
        run: echo "$(<greetings.txt)"
